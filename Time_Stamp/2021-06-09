git add<filename>
: 어떤 파일을 만들어서 입력할 때 사용하는 코드

git status
: 파일의 현재 상태 확인

git init
: Repository 생성

git commit
: 변경 내용 확정

git commit을 누르면 vim 이 열린다
이 화면을 나가기 위해선 ESC를 누르고
:wq   //를 쳐서 나간다.
** wq = write and quit.
